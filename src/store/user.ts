import firebase from "firebase";
import { Ref } from "vue";

export default {
  // actions: {
  //   onSaveImage(state: any, image: Ref<HTMLCanvasElement | null>) {
  //     if (image) {
  //       return firebase
  //         .database()
  //         .ref(`${state.rootState.userId}/`) //Ask Andrei about ts "Property 'rootState' does not exist on type '{ savedImages: { string: HTMLImageElement; }; }"
  //         .push(image)
  //         .catch((err) => alert(err.message));
  //     }
  //   },
  // },
};
